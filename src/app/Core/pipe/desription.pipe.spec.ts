import { DesriptionPipe } from './desription.pipe';

describe('DesriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DesriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
